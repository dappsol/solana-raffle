use crate:: {error, id, state, utils, UploadAccount};
use anchor_lang::{prelude::*, solana_program::clock::UnixTimestamp};
use anchor_spl::token;

impl<'info> BuyTicketAccount<'info> {
    pub fn process(
        &mut self,
        
    )
}